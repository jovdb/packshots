// http://jsfiddle.net/mrbendel/6rbtde5t/1/

function renderImage({
    targetSize,
    spreadSampler,
    config: {
        corners,
    },
}: {
    targetSize: any,
    spreadSampler: any,
    config: {
        corners: [x: number, y: number][];
    }
}) {
    
}
